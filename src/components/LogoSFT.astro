---
import { Image } from 'astro:assets';
import shoftime from '/src/components/stst.png';
---
<Image
{...Astro.props}
      src={shoftime}
      alt="logo"
      style={{
        maxWidth: '100%',
        minWidth: '200px',
        height: 'auto',
      }}
    />
